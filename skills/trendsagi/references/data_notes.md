# Data Notes

## saas_users.preferred_language
A migration adds a `preferred_language` column to `saas_users` for tailoring API docs and onboarding. If user profile endpoints expose language preferences, map them to this field.
